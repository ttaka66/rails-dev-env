version: '3'
services:
  db:
    image: postgres:9.5.4
  app:
    build: .
    command: rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/usr/src/railsapp
    ports:
      - "3000:3000"
    depends_on:
      - db
